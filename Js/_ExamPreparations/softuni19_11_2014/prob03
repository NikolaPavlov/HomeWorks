function solve(input) {
	//regex /<td>[\d\.]+)<\/td>/
	var match;
	var regex = /regexHere/;
	var i;


	for (var i = 2; i < input.length; i++) {
		match = regex.exec(input[i]);
		rows.push((
			price: Number(match)		
	}
}